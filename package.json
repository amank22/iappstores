{
  "name": "iappstores",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter='@iappstores/contracts' build && bun run dev:web",
    "dev:web": "bun run --filter='@iappstores/web' dev",
    "start": "bun run start:web",
    "start:web": "bun run --filter='@iappstores/web' start",
    "build": "bun run --filter='@iappstores/contracts' build && bun run --filter='@iappstores/web' build",
    "rebuild": "bun run --filter='@iappstores/contracts' clean && bun run --filter='@iappstores/contracts' build && bun run --filter='@iappstores/web' clean && bun run --filter='@iappstores/web' build",
    "lint": "bun run --filter='@iappstores/web' lint",
    "typecheck": "bun run --filter='@iappstores/contracts' build && bun run --filter='@iappstores/web' typecheck",
    "test": "bun run --filter='@iappstores/web' test"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "trustedDependencies": ["sharp", "esbuild"],
  "packageManager": "bun@1.4.0"
}
